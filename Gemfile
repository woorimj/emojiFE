source 'https://rubygems.org'

# You may use http://rbenv.org/ or https://rvm.io/ to install and use this version
ruby ">= 2.6.10"

gem 'cocoapods', '~> 1.12'

ENV['GEM_HOME'] = File.expand_path('~/.gem')
ENV['PATH'] = "#{ENV['GEM_HOME']}/bin:#{ENV['PATH']}"

